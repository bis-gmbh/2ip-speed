[Unit]
Description=2ip speed platform
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/speedtest
ExecReload=/bin/kill -HUP $MAINPID
User=nobody
Group=nogroup
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target